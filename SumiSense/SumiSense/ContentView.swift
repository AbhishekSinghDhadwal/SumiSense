import SwiftUI

// Legacy placeholder kept for compatibility with older previews.
struct ContentView: View {
    var body: some View {
        Text("SumiSense")
            .font(SumiTypography.title)
            .padding()
    }
}

#Preview {
    ContentView()
}
